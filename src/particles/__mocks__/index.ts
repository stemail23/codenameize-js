const mockAdjectives = ["quick", "lazy", "happy", "sad"];
const mockNouns = ["fox", "dog", "cat", "mouse"];

export const adjectives = mockAdjectives;
export const nouns = mockNouns;

export default {
	adjective: mockAdjectives,
	noun: mockNouns,
};
